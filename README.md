# RAMMs

A Python package for constructing, visualizing, and analyzing
Reconfigurable Architected Mechanical Metamaterials (RAMMs).

## Installation

```bash
pip install -e .
```

## Example

```python
from ramms import RAMM_Chain
```